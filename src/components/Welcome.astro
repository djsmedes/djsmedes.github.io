---
// import someName from '../assets/someFile.svg';
---

<div id="container">
  <main>
    <section id="hero">
      <h1>Daniel Smedema</h1>
      <section id="links">
        <a class="button" href="/about"> About </a>
        <a class="button" href="https://localhost:4321/TODO">
          This is another link button.
        </a>
      </section>
      <section id="blurb">
        <p>
          This is a blurb with some <code>code (monospaced) text</code>
        </p>
      </section>
    </section>
  </main>
</div>

<style>
  #container {
    font-family:
      Inter, Roboto, "Helvetica Neue", "Arial Nova", "Nimbus Sans", Arial,
      sans-serif;
    height: 100%;
  }
  code {
    font-family:
      ui-monospace, "Cascadia Code", "Source Code Pro", Menlo, Consolas,
      "DejaVu Sans Mono", monospace;
    font-weight: 400;
    background: darkslateblue;
    color: greenyellow;
    padding: 0.2rem 0.4rem;
    border-radius: 0.25rem;
  }

  main {
    height: 100%;
    display: flex;
    justify-content: center;
  }

  #hero {
    display: flex;
    align-items: start;
    flex-direction: column;
    justify-content: center;
    padding: 16px;
  }

  #hero section {
    margin-bottom: 1rem;
  }

  #links {
    display: flex;
    gap: 16px;
  }

  #links a.button {
    display: flex;
    align-items: center;
    padding: 10px 12px;
    text-decoration: none;
    color: black;
    background: linear-gradient(to right, white 33%, black 66%);
    background-size: 300% 100%;
    background-position: left;
    transition:
      background-position 0.5s ease,
      border-radius 0s ease 0.5s;
  }

  @keyframes fadeThruMidpoint {
    0% {
      color: black;
    }

    50% {
      color: greenyellow;
    }

    100% {
      color: white;
    }
  }

  #links a.button:hover {
    color: white;
    background-position: right;
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.12),
      inset 0 -2px 0 rgba(0, 0, 0, 0.24);
    border-radius: 10px;
    animation: fadeThruMidpoint 0.5s forwards;
    transition:
      background-position 0.5s ease,
      border-radius 0s ease;
  }

  /* 
  For reference - if I want to include an svg in a link, might need to mess with sizing and spacing
  #links a svg {
    height: 1em;
    margin-left: 8px;
  }
  */

  h1 {
    font-size: 22px;
    margin-top: 0.25em;
  }

  h2 {
    margin: 0 0 1em;
    font-weight: normal;
    color: #111827;
    font-size: 20px;
  }

  p {
    color: #4b5563;
    font-size: 16px;
    line-height: 24px;
    letter-spacing: -0.006em;
    margin: 0;
  }

  @media screen and (max-width: 768px) {
    #container {
      display: flex;
      flex-direction: column;
    }

    #hero {
      display: block;
      padding-top: 10%;
    }

    #links {
      flex-wrap: wrap;
    }

    #links a.button {
      padding: 14px 18px;
    }

    h1 {
      line-height: 1.5;
    }
  }
</style>
